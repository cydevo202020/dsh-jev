# Changelog

## 0.1.0（2026-09-19）
- 首次入库。
- 三条通道：模型可见工具 `jev_judge`、其它插件可 await 的 `jev` 服务、以及每会话的工具闸门
  （通用工具一套、computer-use／browser-use 专用一套）。
- 工具闸门：off／shadow／enforce 三种模式，按会话热插拔；模式表落
  `~/.dsh/jev-gate/state.json`（`*` 是所有未显式设置的会话的兜底），判定流水落
  `shadow-YYYY-MM-DD.jsonl`。
- 减少调用：只读工具直接跳过（回溯 408 次真实判定，省掉 46.8%）、形状缓存、
  端点预算公平分配（修掉"第一个端点吃光超时、备选端点被饿死"）、参数裁剪（`gateArgChars`）。
- 放权：判定器拿不准时交给带上下文的 LLM 分类器（U1–U5 触发条件，默认影子模式）。
- 与守卫插件合并：暴露 `jevGate` 服务，其它插件把规则挂进同一次 Jev 调用，合并口径"最严的赢"。
- 加载韧性：同名服务／工具已被上一代实例占用时只写诊断，不再让整个装配项变成 failed。
